const fs = require('fs'); const archiver = require('archiver'); console.log('Starting DXT build...'); const output = fs.createWriteStream('google-apps-script-mcp-v1.1.0-clasp.dxt'); const archive = archiver('zip', { zlib: { level: 9 } }); archive.pipe(output); archive.directory('src/', 'src/'); archive.directory('config/', 'config/'); archive.file('package.json', { name: 'package.json' }); archive.file('manifest.json', { name: 'manifest.json' }); archive.file('README.md', { name: 'README.md' }); archive.file('.env.example', { name: '.env.example' }); archive.finalize(); console.log('DXT build completed!'); 
